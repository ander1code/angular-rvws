import { CaptextDirective } from './captext.directive';

describe('CaptextDirective', () => {
  it('should create an instance', () => {
    const directive = new CaptextDirective();
    expect(directive).toBeTruthy();
  });
});
